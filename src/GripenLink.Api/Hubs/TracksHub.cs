using Microsoft.AspNetCore.SignalR;

namespace GripenLink.Api.Hubs;

/// <summary>
/// Hub que transmite as pistas em tempo real para os clientes (mapa vivo / MDIs).
/// </summary>
public sealed class TracksHub : Hub
{
}
